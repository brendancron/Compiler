trait Add {
    fn add(self, other);
}

// Int+int and string+string are handled by the interpreter directly (a
// shortcut before dispatch_binop runs), so no impl is needed here.

impl Add for list {
    fn add(self, other) {
        self.push(other);
        return self;
    }
}
