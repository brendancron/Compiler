type StringBuilder {
    parts: List<string>,
    length: int,
}

fn new(): StringBuilder {
    return new StringBuilder { parts: [], length: 0 };
}

impl StringBuilder {
    fn append(self, s: string) {
        self.parts.push(s);
        self.length = self.length + s.len();
    }
    fn append_int(self, n: int) {
        self.append(str(n));
    }
    fn append_line(self, s: string) {
        self.append(s);
        self.append("\n");
    }
    fn build(self): string {
        var out = "";
        for (part in self.parts) { out = out + part; }
        return out;
    }
    fn len(self): int {
        return self.length;
    }
    fn clear(self) {
        self.parts = [];
        self.length = 0;
    }
}
