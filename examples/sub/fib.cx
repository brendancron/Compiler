meta {
    fn fib(n) {
        if (n == 1) {
            return 1;
        } else if (n == 0) {
            return 1;
        }
        return fib(n-1) + fib(n-2);
    }
    var fib_5 = fib(5);
    gen var x = fib_5;
}

print(x);
