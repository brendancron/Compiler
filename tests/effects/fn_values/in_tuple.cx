effect E { ctl op(): int; }

fn doubled() -> <E> int { return op() * 2; }

run {
    var pair = (() => { return doubled(); }, 5);
    print(str(pair.0()));
    print(str(pair.1));
} handle E {
    ctl op() { resume 7; }
}
