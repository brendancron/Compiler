effect Exception {
    ctl throw(msg: String): Unit;
}

fn divide(n, d) {
    if (d == 0) {
        throw("division by zero");
    }
    return n / d;
}

run {
    print(divide(10, 2));
    print(divide(5, 0));
    print("unreachable");
} handle Exception {
    ctl throw(msg) {
        print(msg);
    }
}
