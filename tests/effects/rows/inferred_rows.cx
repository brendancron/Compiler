effect logger { fn log(msg: string): unit; }
effect counter { fn tick(): unit; }

fn greet(name: string) { log("hi " + name); tick(); }
fn indirect() { greet("x"); }
fn pure_fn(n: int): int { return n + 1; }

print(typeof(log));
print(typeof(greet));
print(typeof(indirect));
print(typeof(pure_fn));
