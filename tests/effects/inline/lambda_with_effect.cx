effect Log {
    fn log(msg: String): Unit;
}

handler Print : Log {
    fn log(msg) {
        print(msg);
    }
}

fn call_twice(f) {
    f();
    f();
}

run {
    call_twice(fn() { log("from lambda"); });
} handle Print;
