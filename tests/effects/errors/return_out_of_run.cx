effect logic { ctl choose(): int; }
fn pick() -> int {
    run {
        var n = choose();
        if (n == 2) { return n * 10; }
    } handle logic { ctl choose() { resume 1; resume 2; } }
    return 7;
}
print(pick());
