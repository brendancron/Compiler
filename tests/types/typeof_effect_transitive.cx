// Expected: ctl effects propagate transitively up the call chain.

effect Stream {
    ctl yield(i: Int): Unit;
}

fn range(low: Int, high: Int) {
    var i = low;
    while (i < high) {
        yield(i);
        i += 1;
    }
}

fn double_range(n: Int) {
    range(0, n);
    range(n, n * 2);
}

print(typeof(range));
print(typeof(double_range));
