// Expected: effect row appears postfix on the return type.

effect Stream {
    ctl yield(i: Int): Unit;
}

fn range(low: Int, high: Int) {
    var i = low;
    while (i < high) {
        yield(i);
        i += 1;
    }
}

print(typeof(range));
