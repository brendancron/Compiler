// Expected: effect row lists all effects, sorted alphabetically.

effect Io {
    ctl emit(val: Int): Unit;
    ctl request(): Int;
}

fn ping_pong(n: Int) {
    emit(n);
    var r = request();
    emit(r);
}

print(typeof(ping_pong));
