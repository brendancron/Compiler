// A function calling both fn and ctl ops — only the ctl effect appears in its type row.

effect Mixed {
    fn log(msg: String): Unit;
    ctl yield(i: Int): Unit;
}

fn produce(n: Int) {
    log("start");
    yield(n);
    log("end");
}

print(typeof(produce));
