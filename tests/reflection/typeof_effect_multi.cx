// Two operations of one effect are one entry: what a row records is what
// has to be handled.

effect io {
    ctl emit(val: int): unit;
    ctl request(): int;
}

fn ping_pong(n: int) {
    emit(n);
    var r = request();
    emit(r);
}

print(typeof(ping_pong).name);
