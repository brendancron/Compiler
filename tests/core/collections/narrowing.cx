type Stack { n: int }

impl Stack {
    fn push(self, v: int) {
        self.n = self.n + v;
    }
}

var xs = [1, 2, 3];
xs.push(4);
print(xs.len(), xs[3], typeof(xs).name);

var ys = [1, 2];
print(ys.len(), typeof(ys).name);

var s = new Stack { n: 0 };
s.push(5);
print(s.n);
