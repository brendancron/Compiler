var pairs = [(1, "a"), (2, "b"), (3, "c")];

var sum = 0;
for ((n, s) in pairs) {
    sum = sum + n;
    print(s);
}
print(sum);

var triples: List<(int, int, int)> = [(1, 2, 3), (4, 5, 6)];
for ((a, b, c) in triples) {
    print(a + b + c);
}

for (whole in pairs) {
    print(whole.1);
}
