class Vec2(x: Int, y: Int)

// Scalar multiplication: Vec2 * Int
impl Mul for Vec2 {
    fn mul(scalar: Int): Vec2 {
        return Vec2(this.x * scalar, this.y * scalar);
    }
}

var v = Vec2(2, 4);
var scaled = v * 3;
print(scaled.x);
print(scaled.y);
