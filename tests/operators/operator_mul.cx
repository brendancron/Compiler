type Vec2 {
    x: int,
    y: int
}

op *(a: Vec2, b: int) -> Vec2 {
        return new Vec2 { x: a.x * b, y: a.y * b };
}

var v = new Vec2 { x: 2, y: 4 };
var scaled = v * 3;
print(scaled.x);
print(scaled.y);
