class Vec2(x: Int, y: Int)

impl Add for Vec2 {
    fn add(other: Vec2): Vec2 {
        return Vec2(this.x + other.x, this.y + other.y);
    }
}

var v1 = Vec2(1, 1);
var v2 = Vec2(1, 1);
var v3 = Vec2(1, 1);
var result = v1 + v2 + v3;
print(result.x);
print(result.y);
