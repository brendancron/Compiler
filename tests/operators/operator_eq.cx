type Point {
    x: int,
    y: int
}

op ==(a: Point, b: Point) -> bool {
        return a.x == b.x && a.y == b.y;
}

var p1 = new Point { x: 1, y: 2 };
var p2 = new Point { x: 1, y: 2 };
var p3 = new Point { x: 3, y: 4 };
print(p1 == p2);
print(p1 == p3);
