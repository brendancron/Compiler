class Point(x: Int, y: Int)

impl Eq for Point {
    fn eq(other: Point): Bool {
        return this.x == other.x && this.y == other.y;
    }
}

var p1 = Point(1, 2);
var p2 = Point(1, 2);
var p3 = Point(3, 4);
print(p1 == p2);
print(p1 == p3);
