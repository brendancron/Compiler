class Vec2(x: Int, y: Int)

impl Add for Vec2 {
    fn add(other: Vec2): Vec2 {
        return Vec2(this.x + other.x, this.y + other.y);
    }
}

var v1 = Vec2(1, 2);
var v2 = Vec2(3, 4);
var v3 = v1 + v2;
print(v3.x);
print(v3.y);
