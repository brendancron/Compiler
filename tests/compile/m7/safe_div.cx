enum Option {
  Some(Int),
  None
}

fn safe_div(a: Int, b: Int): Option {
  if (b == 0) {
    return Option::None;
  }
  return Option::Some(a / b);
}

var r = safe_div(10, 2);
match r {
  Option::Some(x) => { print(to_string(x)); },
  Option::None => { print(to_string(-1)); },
}
